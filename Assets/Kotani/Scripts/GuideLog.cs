using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GuideLog : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("十字キーとWASDキーで移動");
        Debug.Log("スペースでジャンプ");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
